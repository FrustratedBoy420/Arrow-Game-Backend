{
  "name": "arrow-escape-backend",
  "version": "2.0.0",
  "description": "Multiplayer Backend for Arrow Escape Game (Vercel Serverless)",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.9",
    "pusher": "^5.2.0"
  },
  "devDependencies": {
    "vercel": "^41.6.0"
  }
}
